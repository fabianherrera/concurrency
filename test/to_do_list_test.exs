defmodule ToDoListTest do
  use ExUnit.Case
  doctest ToDoList

  # @module Concurrency.ToDoList
  #
  # test "get initial value of %{} once server is initialized via value function" do
  #   pid = @module.start()
  #   assert @module.value(pid) == %{}
  # end
  #
end
#update