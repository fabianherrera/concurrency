defmodule Concurrency.CalculaterServerTest do
  use ExUnit.Case
  # doctest Concurrency.CalculatorServer
  #
  # @module Concurrency.CalculatorServer
  #
  # test "adding a value to current state via add function" do
  #   pid = @module.start()
  #   assert @module.add(pid, 50) == 50
  # end
  #
  # test "substracting a value to current state via sub function" do
  #   pid = @module.start()
  #   assert @module.sub(pid, 50) == 50
  # end
  #
  # test "returns a value of 0, once initializated via value function" do
  #   pid = @module.start()
  #   assert @module.value(pid) == 0
  # end

end
